# Xiao Cai Homepage Deploy Package

This folder is ready for GitHub Pages.

Copy all files and folders in this directory to the root of your `Cdawn628.github.io` repository, then commit and push.

Expected root structure:

```text
index.html
assets/
images/
resume/
.nojekyll
README_DEPLOY.md
```

After pushing, the homepage should be available at:

```text
https://Cdawn628.github.io/
```

The main page is `index.html`. The nested page `resume/project-page/cx.html` is kept with the same content and path for backup/direct access.
